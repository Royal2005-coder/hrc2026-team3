# Mẫu RGB-D

## Vai trò

Thư mục này chứa sample RGB-D runtime làm bằng chứng camera readiness và semantic pose runtime.

## Nhóm sample chính

- `sample_rgb_*`
- `sample_depth_*`
- `sample_depth_vis_*`

Ý nghĩa:

- sample capture gốc cho 4 camera runtime

## Nhóm sample semantic pose

- `semantic_pose_rgb_head_left.png`
- `semantic_pose_depth_head_left.npy`
- `semantic_pose_depth_vis_head_left.png`

Ý nghĩa:

- sample được dùng trong runtime semantic final trên camera chính `head_left`
